redis-cli -a 123456  --cluster create --cluster-replicas 1 127.0.0.1:6000 127.0.0.1:6001 127.0.0.1:6002 127.0.0.1:7000 127.0.0.1:7001 127.0.0.1:7002 
这里注意以下几点：

network_mode设置为host，否则在创建集群时，会一直等待而不会创建成功
volumes挂载点必须覆盖容器内部配置，大家也可以考虑挂载redis持久化的数据文件

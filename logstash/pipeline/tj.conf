input {
    elasticsearch {
        hosts => ["http://172.16.210.243:9200"]
        index => "tj"
        docinfo => true
    }
}
output {
        elasticsearch {
            hosts => ["http://es1:9200"]
            index => "tj"
        }
}
